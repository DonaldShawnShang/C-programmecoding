#include<iostream>
#include<vector>
#include<algorithm>
#include<cmath>
#include<string>
using namespace std;
vector<string> bn;
vector<string> gn;
int main()
{
    string a,b;
    while(cin>>a&&getchar()!='\n')
    {
        bn.push_back(a);
    }
    while(cin>>b&&getchar()!='\n')
    {
        gn.push_back(b);
    }
    
}